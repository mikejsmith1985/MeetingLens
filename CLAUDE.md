# meetinglens — Forge Agent Instructions

> Auto-generated by Forge Terminal. Claude Code reads this file automatically
> at every session start. All workflow rules below are binding — read them
> before beginning any task.

@.specify/memory/constitution.md

<!-- SPECKIT START -->
## Active Feature: 001-meeting-capture

- Spec: `specs/001-meeting-capture/spec.md`
- Plan: `specs/001-meeting-capture/plan.md`
- Stack: Python 3.11 desktop/tray app — `keyboard` (global hotkeys), `pyttsx3` (offline SAPI5 speech), `Pillow` (screen capture), `pystray` (tray), `pyperclip` (clipboard); PyInstaller folder-drop.
- Design: adapter seam per OS touchpoint (`src/meetinglens/adapters/`) so domain logic (session/config/prompt/notes/handoff) is mock-unit-testable in <10ms; real Windows APIs used in integration tests (no Cypress — no web UI).
<!-- SPECKIT END -->
